<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <style>
    body {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0px;
      background-color: #555;
    }

    

    h1 {
      color: hsl(286, 48%, 50%);
      font-family: 'Roboto', sans-serif;
      font-size: 3rem;
      text-align: center;
      text-shadow: 2px 2px 4px #343434;
      margin-bottom: 2rem;
    }

    form {
      background-color: #333;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      margin: 1rem;
    }

    label {
      color: #8e44ad;
      font-family: 'Roboto', sans-serif;
      display: block;
      margin-bottom: 0.5rem;
      
    }

    input[type="text"],
    input[type="password"] {
      width: 100%;
      padding: 0.5rem;
      margin-bottom: 1rem;
      box-sizing: border-box;
      border: 1px solid #8e44ad;
      border-radius: 4px;
      transition: all 0.2s ease-in-out;
    }

    input[type="text"]:focus,
    input[type="password"]:focus {
      box-shadow: 0 4px 8px rgba(142, 68, 173, 0.2);
    }

    input[type="submit"],
    input[type="button"] {
      background-color: #8e44ad;
      color: #fff;
      padding: 0.5rem 1rem;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: all 0.2s ease-in-out;
    }

    input[type="submit"]:hover,
    input[type="button"]:hover {
      background-color: #6c3483;
    }

    input[type="button"] {
      margin-top: 1rem;
    }
  </style>

</head>

<body>

  <div class="main">
    <form method="POST" action="/login">
      <h1>Login</h1> <label for="username">Username</label> <input type="text" name="username" id="username" required>
      <br> <label for="password">Password</label> <input type="password" name="password" id="password" required> <br>
      <input type="submit" value="Login">
    </form>
    <form method="GET" action="/register"> <input type="button" value="Register" onclick="location.href='/register';">
    </form>
  </div>
</body>

</html>